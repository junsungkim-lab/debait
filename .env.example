# --- Web ---
BASE_URL=https://your-public-https-domain.com
SESSION_SECRET=change-me-very-long-random
WEBHOOK_SECRET=change-me-very-long-random

# --- Telegram ---
TELEGRAM_BOT_TOKEN=123456:ABCDEF...

# --- Encryption for BYOK ---
# Generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MASTER_KEY=YOUR_FERNET_KEY_HERE

# --- Optional provider defaults (not required for BYOK) ---
DEFAULT_PROVIDER=openai   # openai|anthropic
DEFAULT_MODEL=openai:gpt-4o-mini
