version: "3.9"

services:
  web:
    build: .
    container_name: ai-multimodel-webapp
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app.db:/app/app.db
    restart: unless-stopped