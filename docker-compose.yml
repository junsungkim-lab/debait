version: "3.9"

services:
  web:
    build: .
    container_name: ai-multimodel-webapp
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_URL=sqlite:////data/app.db
    volumes:
      - ./data:/data
    restart: unless-stopped
